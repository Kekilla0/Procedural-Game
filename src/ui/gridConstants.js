// Shared base cell size for anything grid-based in the UI — the inventory
// grid and the character-sheet equipment grid both use this, so a 1x1
// inventory cell and an equipment slot are always the same size by
// construction, not by two numbers that happen to match.
export const GRID_CELL_SIZE = 40;
export const GRID_CELL_GAP = 6;
